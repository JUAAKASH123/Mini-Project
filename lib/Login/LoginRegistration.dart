import 'package:flutter/material.dart';

class LoginRes extends StatelessWidget {
  const LoginRes({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
